import { createContext, useContext, useEffect, useState } from 'react'; import { authService } from '../services/auth';
const AuthContext=createContext(null); export function AuthProvider({children}){const [user,setUser]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{authService.refresh().then(x=>setUser(x.usuario)).catch(()=>setUser(null)).finally(()=>setLoading(false));},[]);const value={user,loading,login:async(...a)=>{const x=await authService.login(...a);setUser(x.usuario);return x;},register:async(...a)=>{const x=await authService.register(...a);setUser(x.usuario);return x;},logout:async()=>{await authService.logout();setUser(null);}};return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>};export const useAuth=()=>useContext(AuthContext);
