let accessToken = null;
async function request(path, options = {}) { const response = await fetch(path, { ...options, credentials: 'include', headers: { 'content-type': 'application/json', ...options.headers } }); const data = response.status === 204 ? null : await response.json(); if (!response.ok) throw new Error(data?.erro || 'Não foi possível concluir a autenticação.'); if (data?.accessToken) accessToken = data.accessToken; return data; }
export const authService = { register: (nome,email,senha) => request('/api/auth/register',{method:'POST',body:JSON.stringify({nome,email,senha})}), login: (email,senha) => request('/api/auth/login',{method:'POST',body:JSON.stringify({email,senha})}), refresh: () => request('/api/auth/refresh',{method:'POST'}), logout: async () => { try { await request('/api/auth/logout',{method:'POST'}); } finally { accessToken=null; } }, me: () => request('/api/auth/me',{headers:accessToken?{authorization:`Bearer ${accessToken}`}:{}}), token: () => accessToken };
